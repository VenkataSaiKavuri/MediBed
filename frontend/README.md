# MedBeds Frontend (React)

Not started yet — this is a Day 2 task ("Build frontend login/signup screens").

When we get there, scaffold it locally with:
```bash
npx create-react-app frontend
# or, if you prefer Vite (faster, recommended):
npm create vite@latest frontend -- --template react
```
